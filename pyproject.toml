[project]
name = "pageindex"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["litellm", "pymupdf", "PyPDF2", "python-dotenv", "pyyaml"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pageindex"]
